import React from 'react';

const Footer = () => {
    return (
        <footer className="footer">
            &copy;{new Date().getFullYear()} Fridge Friend
        </footer>
    );
};

export default Footer;